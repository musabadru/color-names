{
  "name": "color-corpus",
  "private": true,
  "scripts": {
    "dev": "pnpm -F web dev",
    "build": "pnpm -r build",
    "build:web": "pnpm -F apps-web build",
    "build:vercel": "pnpm run build:web && node scripts/copy-vercel-output.mjs"
  }
}
